export type ProjectsArrayType = {
    id: number;
    title: string;
    descriptions: string;
    image: string;
    url?: string;
};
export type WrapperType = {
    direction?: string,
    justifyContent?: string,
    align?: string,
    wrap?: string,
}